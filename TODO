Définir la structure qui gère les cellules:
	-> Réfléchir à l'interface.
	-> Permettre la sauvegarde vers une std::string.
	-> Permettre le chargement depuis une std::string.

Définit la structure qui gère les vaisseaux sanguins:
	-> Réfléchir à l'interface :
		-> Faire une structure pour chaque case.
		-> Faire une classe qui représente toutes les cases de chaque vaisseau sanguin.
	-> Permettre la sauvegarde vers une std::string.
	-> Permettre le chargement depuis une std::string.

Coder le moteur de tilemapping hexagonal:
	-> Réfléchir à l'interface:
		-> Permettre l'accès aux cellules et vaisseaux sanguins.
		-> Permettre leur modification.
	-> Définir un format de fichier.
	-> Implémenter le chargement.
	-> Implémenter la sauvegarde.
	-> Implémenter le dessin des tiles.
	-> Implémenter le scroll.
	-> Implémenter le test de colision.

